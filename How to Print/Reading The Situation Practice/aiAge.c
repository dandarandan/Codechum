/*
Instruction:
1. Manually calculate Ai's age by hand by subtracting the year of its invention, 1955, to the current year 2020.
2. This time around, try writing the whole code on your own, including the basic C program structure to practice.

Expected Output:

A single line containing an integer.
65
*/

#include <stdio.h>
int main (void) {

    printf("%d", 2020 - 1955);
    return 0;
}